# Датасорс Prometheus по умолчанию: панели codelens.json используют дефолтный datasource.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
